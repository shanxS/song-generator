function externalFeatures = getExternalFeatures(          \
                                       numTrainingData    \
                                     , timeBinNumber      \
                                     , frequencyBinNumber)
    % Rows hold instances, columns hold features
    externalFeatures = zeros(numTrainingData, 1);
    
end